8. Run job inside a container

7. Testing artifact actions

6. Ansible is already available in base ubuntu image in GH action

5. ssh stuff in action

4. Change primary branch from master to main

3. Test whether GitHub Action default image contain ssh (it does)

2. Silly me commit from wsl, now is a proper test

1. Test whether a copied gpg folder from wsl to windows will still work